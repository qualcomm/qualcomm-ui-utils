# @qualcomm-ui/codemod

Codemod utilities for automatic QUI package migrations

## How to

Run the following command to see the available codemods and instructions:

```shell
npx @qualcomm-ui/codemod migrate --help
```
